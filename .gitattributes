# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to be normalized and converted to CRLF on checkout.
*.css text
*.js text
*.jsx text
